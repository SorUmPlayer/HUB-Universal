--[[ 
    Blue Lock - Sor_UmPlayer HUB (Reescrito)
    - Aimbot: olha para a bola sem teleportar
    - Reach: aumenta a hitbox da bola (não move a bola)
    - ESP: esfera mostrando o alcance na bola
]]

local _ENV = getgenv()
local player = game.Players.LocalPlayer
local camera = workspace.CurrentCamera
local runService = game:GetService("RunService")
local replicatedStorage = game:GetService("ReplicatedStorage")

-- Variáveis globais
_ENV.MagnetRange = 20
_ENV.MagnetAtivo = false
_ENV.AutoLockBall = false
_ENV.ShowESP = false

-- Notificação
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "Sor_UmPlayer Hub",
    Text = "Bem-vindo! Script recarregado.",
    Duration = 5
})

-- Carregar WindUI
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
local Window = WindUI:CreateWindow({
    Title = "🌟 Sor_UmPlayer HUB 🌟",
    Icon = "rbxassetid://10723343321",
    Author = "made by Sor_Gostoso",
    Folder = "CloudHub",
    Size = UDim2.fromOffset(460, 320),
    Transparent = true,
    Theme = "Dark",
    Resizable = true,
    SideBarWidth = 200,
    HideSearchBar = true,
    ScrollBarEnabled = false,
})

-- =============================================
-- FUNÇÕES PARA ENCONTRAR A BOLA E HITBOX
-- =============================================

local function getBallAndHitbox()
    -- Primeiro tenta o caminho conhecido
    local football = workspace:FindFirstChild("Football")
    if football then
        local hitbox = football:FindFirstChild("Hitbox")
        if hitbox and hitbox:IsA("BasePart") then
            return football, hitbox
        end
        -- Se não tiver Hitbox, usa o próprio Football como hitbox
        return football, football
    end

    -- Busca dinâmica por qualquer parte com nome contendo "ball" ou "football"
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") and obj.Name:lower():match("ball") then
            -- Verifica se tem filho Hitbox
            local hitbox = obj:FindFirstChild("Hitbox")
            if hitbox and hitbox:IsA("BasePart") then
                return obj, hitbox
            end
            -- Caso contrário, usa a própria parte
            return obj, obj
        end
    end
    return nil, nil
end

-- Atualiza a referência da hitbox a cada chamada (caso a bola seja recriada)
local function getHitbox()
    local _, hitbox = getBallAndHitbox()
    return hitbox
end

local function getBallPart()
    local ball, _ = getBallAndHitbox()
    return ball
end

-- Verifica se a bola está com outro jogador (ocupada)
local function isBallOccupied()
    local hitbox = getHitbox()
    if not hitbox then return false end
    local parent = hitbox.Parent
    if parent and parent:FindFirstChild("Humanoid") and parent.Name ~= player.Name then
        return true
    end
    -- Verifica se está na mão de IA
    if hitbox:IsDescendantOf(workspace:FindFirstChild("AI")) then
        return true
    end
    return false
end

-- =============================================
-- AIMBOT (somente rotação, sem mover o personagem)
-- =============================================

local aimbotConnection = nil
local hasBallValue = nil

local function updateAimbot()
    local character = player.Character
    if not character then return end
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    local humanoid = character:FindFirstChild("Humanoid")
    if not rootPart or not humanoid then return end

    local ball = getBallPart()
    if not ball then return end

    -- Só olha se o jogador NÃO está com a bola
    if hasBallValue and hasBallValue.Value == true then
        return
    end

    -- Direção para a bola (mantém altura do chão)
    local targetPos = Vector3.new(ball.Position.X, rootPart.Position.Y, ball.Position.Z)
    local direction = (targetPos - rootPart.Position).Unit
    if direction.Magnitude < 0.1 then return end

    -- Aplica rotação suave (opcional) – aqui apenas definimos o CFrame para olhar
    rootPart.CFrame = CFrame.new(rootPart.Position, rootPart.Position + direction)
end

local function startAimbot()
    if aimbotConnection then return end
    aimbotConnection = runService.RenderStepped:Connect(updateAimbot)
end

local function stopAimbot()
    if aimbotConnection then
        aimbotConnection:Disconnect()
        aimbotConnection = nil
    end
end

-- Detecta se o jogador tem a bola (para desativar o aimbot)
local function setupHasBall()
    local model = workspace:FindFirstChild(player.Name)
    if model then
        local values = model:FindFirstChild("Values")
        if values then
            hasBallValue = values:FindFirstChild("HasBall")
        end
    end
    -- Fallback: procurar em qualquer lugar
    if not hasBallValue then
        for _, v in ipairs(workspace:GetDescendants()) do
            if v.Name == "HasBall" and v:IsA("BoolValue") and v.Parent and v.Parent.Name == player.Name then
                hasBallValue = v
                break
            end
        end
    end
    if hasBallValue then
        hasBallValue:GetPropertyChangedSignal("Value"):Connect(function()
            if hasBallValue.Value then
                print("Aimbot pausado: você pegou a bola.")
                if _ENV.AutoLockBall then stopAimbot() end
            else
                print("Aimbot ativado: você perdeu a bola.")
                if _ENV.AutoLockBall then startAimbot() end
            end
        end)
    else
        warn("HasBall não encontrado. O aimbot pode não funcionar corretamente.")
    end
end

-- =============================================
-- REACH (aumentar a hitbox da bola)
-- =============================================

local reachLoop = nil

local function applyReach()
    local hitbox = getHitbox()
    if not hitbox then return end

    if _ENV.MagnetAtivo then
        -- Redimensiona a hitbox para o tamanho definido
        local range = _ENV.MagnetRange
        hitbox.Size = Vector3.new(range, range, range)
        hitbox.CanCollide = false -- evita que a bola trave em objetos
    else
        -- Restaura o tamanho original (aproximado)
        hitbox.Size = Vector3.new(1.4, 1.4, 1.4)
        hitbox.CanCollide = true
    end
end

local function startReachLoop()
    if reachLoop then return end
    reachLoop = task.spawn(function()
        while _ENV.MagnetAtivo do
            applyReach()
            task.wait(0.1)
        end
        -- Quando desativado, restaura uma vez
        applyReach()
    end)
end

local function stopReachLoop()
    _ENV.MagnetAtivo = false
    if reachLoop then
        task.cancel(reachLoop)
        reachLoop = nil
    end
    applyReach() -- restaura tamanho
end

-- =============================================
-- ESP (esfera ao redor da hitbox da bola)
-- =============================================

local espSphere = nil

local function updateESP()
    local hitbox = getHitbox()
    if not hitbox then
        if espSphere then espSphere:Destroy() espSphere = nil end
        return
    end

    if _ENV.ShowESP then
        if not espSphere or espSphere.Parent == nil then
            espSphere = Instance.new("SelectionSphere")
            espSphere.Name = "ReachVisual"
            espSphere.Color3 = Color3.fromRGB(0, 255, 100)
            espSphere.Transparency = 0.7
            espSphere.Adornee = hitbox
            espSphere.Parent = hitbox
        end
        -- Atualiza o tamanho da esfera de acordo com o tamanho da hitbox
        espSphere.Size = hitbox.Size
    else
        if espSphere then
            espSphere:Destroy()
            espSphere = nil
        end
    end
end

local espLoop = nil
local function startESPLoop()
    if espLoop then return end
    espLoop = task.spawn(function()
        while _ENV.ShowESP do
            updateESP()
            task.wait(0.3)
        end
        -- Limpa ao desativar
        if espSphere then
            espSphere:Destroy()
            espSphere = nil
        end
    end)
end

local function stopESPLoop()
    _ENV.ShowESP = false
    if espLoop then
        task.cancel(espLoop)
        espLoop = nil
    end
    if espSphere then
        espSphere:Destroy()
        espSphere = nil
    end
end

-- =============================================
-- INFINITE STAMINA
-- =============================================

local staminaLoop = nil
local function startStamina()
    if staminaLoop then return end
    staminaLoop = task.spawn(function()
        while true do
            local stats = player:FindFirstChild("PlayerStats")
            if stats then
                local stamina = stats:FindFirstChild("Stamina")
                if stamina then
                    stamina.Value = 100
                end
            end
            task.wait(0.5)
        end
    end)
end

-- =============================================
-- SCORE GOAL (mantido igual, mas com correções)
-- =============================================

local selectedOption = nil
local originalPosition = nil

local function storeOriginalPosition()
    local char = player.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        if root then originalPosition = root.Position end
    end
end

local function teleportTo(pos)
    local char = player.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(pos)
        end
    end
end

local function fireShoot(targetPos)
    local args = { [1] = 110, [4] = targetPos }
    local shootEvent = replicatedStorage:FindFirstChild("Packages") and 
                       replicatedStorage.Packages:FindFirstChild("Knit") and
                       replicatedStorage.Packages.Knit:FindFirstChild("Services") and
                       replicatedStorage.Packages.Knit.Services:FindFirstChild("BallService") and
                       replicatedStorage.Packages.Knit.Services.BallService:FindFirstChild("RE") and
                       replicatedStorage.Packages.Knit.Services.BallService.RE:FindFirstChild("Shoot")
    if shootEvent then
        shootEvent:FireServer(unpack(args))
    else
        warn("Evento de chute não encontrado. Verifique o caminho.")
    end
end

-- =============================================
-- INTERFACE (WindUI)
-- =============================================

local mainTab = Window:Tab({ Title = "MAIN", Icon = "home" })
local sectionMain = mainTab:Section({ Title = "Main Options", TextXAlignment = "Left" })

-- Auto Lock Ball
sectionMain:Toggle({
    Title = "Auto Lock Ball",
    Desc = "Olha automaticamente para a bola",
    Callback = function(state)
        _ENV.AutoLockBall = state
        if state then
            if not hasBallValue then setupHasBall() end
            startAimbot()
        else
            stopAimbot()
        end
    end
})

-- Infinite Stamina
sectionMain:Button({
    Title = "Infinite Stamina",
    Desc = "Mantém a stamina em 100",
    Callback = function()
        startStamina()
    end
})

-- Select Team
sectionMain:Dropdown({
    Title = "Select your team",
    Desc = "Escolha Home ou Away",
    Options = {"Home", "Away"},
    Callback = function(option)
        selectedOption = option
    end
})

-- Score Goal
sectionMain:Button({
    Title = "Score Goal",
    Desc = "Faz um gol automático",
    Callback = function()
        if not selectedOption then
            warn("Selecione um time primeiro!")
            return
        end
        local targetPos = selectedOption == "Home" and Vector3.new(-234, 11, -47) or Vector3.new(311, 11, -50)
        storeOriginalPosition()
        teleportTo(targetPos)
        task.wait(0.2)
        fireShoot(targetPos)
        task.wait(0.2)
        if originalPosition then
            teleportTo(originalPosition)
        end
    end
})

-- Seção Reach
local sectionReach = mainTab:Section({ Title = "Legit Reach", TextXAlignment = "Left" })

sectionReach:Slider({
    Title = "Reach Distance",
    Desc = "Tamanho da hitbox da bola (Max 40)",
    Step = 1,
    Value = { Min = 1, Max = 40, Default = 20 },
    Callback = function(value)
        _ENV.MagnetRange = value
        if _ENV.MagnetAtivo then
            applyReach() -- atualiza imediatamente
        end
    end
})

sectionReach:Toggle({
    Title = "Enable Reach",
    Desc = "Aumenta a hitbox da bola",
    Callback = function(state)
        _ENV.MagnetAtivo = state
        if state then
            startReachLoop()
        else
            stopReachLoop()
        end
    end
})

sectionReach:Toggle({
    Title = "Show Reach ESP",
    Desc = "Mostra a área da hitbox na bola",
    Callback = function(state)
        _ENV.ShowESP = state
        if state then
            startESPLoop()
        else
            stopESPLoop()
        end
    end
})

-- =============================================
-- INICIALIZAÇÃO
-- =============================================

-- Tenta encontrar HasBall ao carregar
setupHasBall()

-- Inicializa a hitbox com tamanho padrão (caso já esteja ativo)
task.wait(1)
applyReach()

print("Script recarregado com sucesso! Aproveite.")
